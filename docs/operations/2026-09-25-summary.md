# Итог работы за 25 сентября 2026

Последняя прошитая версия на момент записи: **4671e1b**.
Это итог текущего этапа проекта, включая изменения из сегодняшнего обсуждения;
хеши позволяют отделить реализацию от ранее написанных планов.

## LLM Wiki и Ramble your idea, then build

Исходная цель — голосовой интерфейс к двум идеям: поддерживаемой агентом базе
знаний **LLM Wiki** и переходу от свободно надиктованного замысла к реализации
**Ramble your idea, then build**. Obsidian — общее место хранения и ручной работы.

В первом сценарии уже сохраняются источники, идеи и связанные wiki-страницы.
Во втором работают диктовка, дополнения и подготовка планов/заданий; сам запуск
разработки в репозитории ещё не реализован. Эти границы нельзя смешивать
с готовностью голосового чата или фактом сохранения заметки.

Подробное описание обоих сценариев: [руководство](../voice-knowledge.md).
Исследование первоисточников и сторонних проектов:
[исходный план](../plans/2026-09-25-voice-llm-wiki.md).

## Голос → Obsidian

- Реализовано сохранение потока мысли и дополнений в
  `/home/artem/repos/obsidian/Hermes`: источники, идеи, wiki, планы и задания.
  Пользователь подтвердил работу создания и дополнения настоящей записи.
- «Реализуй идею» пока создаёт задание в vault, а не запускает разработку.
- После публикации заметок очередь обеспечивает commit и push в origin.
  Ошибка push оставляет задание для повтора; пользовательские посторонние
  правки не включаются. Голосовое подтверждение означает сохранение файлов,
  не завершение push.
- Исправлена остановка обработчика очереди после отмены голосового задания.
- Лимит записи увеличен до десяти минут; полная десятиминутная запись на
  физическом устройстве отдельно не подтверждена.

Коммиты: `9e64faf`, `4e0d6c8`, `70c299b`, `055adfa`, `a22ceb7`.
Подробности: [голосовые идеи и Git](../voice-knowledge.md).

## Сеть и экран диктофона

- «Готов» появляется после Wi-Fi и, если VPN включён, готовности WireGuard.
- Подключённые Wi-Fi и VPN обозначаются зелёным.
- При AP или AP+STA WireGuard останавливается и получает статус
  `paused_setup`. После выхода из настройки подключается снова.
- Экран AP явно показывает «Настройка / Точка доступа», QR подключения к
  открытой сети `Hermes-StickS3-Setup-XX` и резервный адрес `192.168.4.1`.
- QR содержит имя Wi-Fi, а не ссылку на страницу. После подключения captive
  DNS и HTTP redirect направляют телефон на setup. Автоматическое открытие
  зависит от телефона; иногда нужно нажать уведомление «Войти в сеть».
- При включённом WireGuard используется полный IPv4-туннель. Голосовой сервер
  может быть в другой подсети за VPN-сервером при правильной серверной маршрутизации.

Коммиты экрана/AP: `4af6828`, `9c7172b`, `848b1ac`, `1ca3838`.
Подробности: [Wi-Fi](../wifi-profiles-sticks3.md), [VPN](../wireguard-sticks3.md).

## Веб-интерфейс

- Три вкладки: **Wi-Fi**, **Настройки сервера**, **VPN**.
- Имена сохранённых сетей видны с галочками; отсутствие сети обозначается
  «Не видна». Пароли не возвращаются.
- Сохранённые адреса сервера и параметры VPN не возвращаются через API,
  включая публичный ключ, порт, keepalive и NTP. Вместо значений —
  признаки наличия; пустое поле оставляет прежнее значение.
- При смене адреса сервера необходимо явно передать токен заново:
  нельзя направить сохранённый токен на другой адрес простой правкой URL.
- Сохранение требует специального HTTP-заголовка против обычных чужих
  HTML-форм. Это не аутентификация владельца.
- Сброс с подтверждением удаляет настройки Wi-Fi, сервера, токен и параметры
  VPN, возвращает таймер сна к 30 секундам. Кнопка сброса имеет те же размеры,
  что сохранение, красный фон и белый текст.
- Полный setup доступен и из локальной Wi-Fi сети, не только из AP.

Коммиты: `77019f3`, `4671e1b`.

## Что проверено

- После изменений настройки: **72 native-теста**, **23 JS-теста** и сборка
  StickS3 прошли.
- После изменения цвета и размера кнопки: 23 JS-теста и сборка прошли.
  В браузере подтверждены одинаковые размеры обеих кнопок и нужные цвета.
- QR независимо декодирован из framebuffer во всех четырёх поворотах.
- В браузере на синтетических данных проверены вкладки, отсутствие значений
  в полях и отсутствие горизонтального переполнения при ширине 360 px.
- В serial подтверждены запуск AP, captive DNS и `paused_setup` после QR-патча.
- Последняя прошивка `4671e1b` загружена; serial подтвердил её версию и
  запуск HTTP setup на порту 80.
- Реальная камера Android, автоматическое открытие captive portal и сохранение
  через новую форму на телефоне не считаются проверенными только по этим тестам.

## Безопасность: пока план, не реализация

Подготовлен [разбор рисков и порядок защиты](../device-security.md):
отзыв токена и VPN peer, минимальные серверные права, пароль владельца для setup,
защищённая AP, проверка подлинности сервера, защита памяти устройства.

В текущей прошивке setup ещё без пароля владельца, AP открыта, настройка по HTTP,
Secure Boot / Flash Encryption / NVS Encryption не включены. Скрытие значений
из API не защищает от использования украденного диктофона или чтения flash.
eFuse, серверные firewall-правила и реальные учётные данные этим этапом не менялись.
