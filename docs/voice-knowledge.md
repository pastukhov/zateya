# Голосовые идеи в Obsidian: LLM Wiki и Ramble your idea, then build

Vault: `/home/artem/repos/obsidian`. Новые записи находятся в `Hermes/`.
Голосовые исходники сохраняются в `sources/`, оформленные мысли в `ideas/`,
связанные темы в `wiki/`, планы и задания в `builds/`. `index.md` открывает
навигацию. `log.md` фиксирует изменения. Существующие папки vault не импортируются.

## Две исходные идеи проекта

Диктофон — голосовой вход в два связанных сценария, а не только голосовой чат
с Codex.

**LLM Wiki:** агент превращает записи в пополняемую базу знаний. Исходная
расшифровка остаётся в `sources/`, оформленная идея — в `ideas/`, а связанные
понятия, сущности и обобщения — в `wiki/`. Страницы связываются с источниками
и друг с другом; индекс и журнал помогают ориентироваться. Дополнение мысли
должно обновлять накопленный материал, а не просто создавать ещё один текст.

**Ramble your idea, then build:** пользователь свободно наговаривает замысел,
затем дополняет и уточняет его. Агент оформляет материал, помогает получить
план и подготавливает задание на реализацию. Наш целевой путь:
диктовка → оформленная идея → уточнения → план → реализация → проверка результата.

Сейчас работает часть до сохранения плана/задания в `builds/` включительно.
Автоматического перехода от голосовой команды к работе в выбранном репозитории,
изменению кода, тестам и выдаче результата ещё нет. Это следующий этап
«then build», а не уже завершённая функция.

Оба сценария используют один vault: идеи и знания можно читать и редактировать
непосредственно в Obsidian. Диктофон служит удобным способом ввода. После
публикации Markdown автоматически попадает в очередь commit/push, описанную ниже.

## Как пользоваться

Просто наговаривайте мысль: служебная фраза «сохрани» не нужна. Можно сказать:

- «Дополни предыдущую мысль: …»
- «Исправь в моей идее: бюджет не 1000, а 2000».
- «Что я говорил о …?»
- «Составь план по предыдущей идее».

Команда «реализуй» пока сохраняет задание в Obsidian: произвольный код не
запускается. Выбор репозитория и исполнитель разработки — отдельный этап.
Вопросы не создают новых идей; их исходная расшифровка остаётся в sources.
Если непонятно, какую мысль дополнять, агент уточнит цель.

Подтверждение «сохранил» формирует сервер только после записи файлов.
Если возник конфликт с ручной правкой, исходник остаётся, а обновление требует
проверки. В архиве соответствующей записи находятся knowledge-context.json,
knowledge-proposal.json и knowledge-result.json. В knowledge-state/knowledge.sqlite3
хранятся журнал применения и предыдущие версии. При рестарте незавершённое
применение продолжается только если файлы не были изменены вручную. Конфликтные
операции автоматически не перезаписываются; их надо разобрать по журналу.
Обновление нескольких файлов не является мгновенно атомарным для Obsidian.

## Подключение vault

Docker Compose использует `OBSIDIAN_HOST_PATH` (по умолчанию указанный выше путь)
и `OBSIDIAN_GROUP_ID` (по умолчанию 1000). Backend остаётся непривилегированным
пользователем; дополнительная группа обеспечивает совместное редактирование.
Перед первым запуском от владельца vault:

```sh
mkdir -p /home/artem/repos/obsidian/Hermes
chmod 2770 /home/artem/repos/obsidian/Hermes
```

В новых каталогах наследуется группа владельца, файлы создаются с режимом 0660.
Не менять владельца всего vault. Для отключения обработки знаний использовать
`VOICE_KNOWLEDGE_ENABLED=false`. Требуется `VOICE_AGENT_PROVIDER=codex` и работающий
host-side agent service. Credentials остаются в прежних локальных env-файлах.

## Источники архитектуры

- [Karpathy LLM Wiki](https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f):
  постоянная база знаний поверх исходников.
- [cerebro](https://github.com/andresbuonaiuto/cerebro), commit
  `d52cbffd8e795a1297a1cc2b8914c61f671432f3`: организация источников и тематических
  страниц, работа с противоречиями.
- [llm-wiki ingest](https://github.com/ddsyasas/llm-wiki/blob/e8dd69ebba0dc7c395c1b8217bb1c30c14e8c84c/packages/core/src/ingest.ts):
  получение контекста и разделение предложения изменений от записи.
- [Rambler](https://github.com/BerkeleyHCI/rambler): оформление смысловых частей речи.

Реализация Python и промпт написаны для Hermes Echo; сторонний код не скопирован.
Дополнительные приложения и плагины Obsidian не требуются.

## Проверка связей

```sh
python -m backend.src.voice_gateway.knowledge /home/artem/repos/obsidian
```

Проверка ничего не меняет. `source_without_note` может означать обычный вопрос
или неудавшуюся обработку, это не автоматическое основание удалить исходник.
Смысловые противоречия оценивает агент; проверка наличия ссылок не доказывает
истинность утверждений.

Лимит записи увеличен до 10 минут (19 200 000 байт PCM). Управление остаётся
push-to-talk: удерживать кнопку во время речи. Фактическая длительная запись
через мобильную сеть требует отдельного аппаратного прогона; отсутствие сети
во время диктовки пока не компенсируется локальным offline-архивом.

## Автоматический commit и push

После публикации заметки и связанных страниц backend оставляет задание в
`Hermes/.sync/`. Сервис `codex-voice-agent` на хосте проверяет очередь каждые
15 секунд, создаёт коммит только перечисленных Markdown-файлов и выполняет
обычный `git push origin HEAD:<текущая ветка>`. Исходник и schema.md включаются.
Короткие последовательные публикации могут объединяться в один коммит.
SSH-ключи остаются на хосте. В unit задаётся `OBSIDIAN_SYNC_VAULT`.

Очередь исключена через `.git/info/exclude` vault. Настройки `.obsidian/` и
остальные подготовленные изменения не входят в автоматические коммиты.
При сбое push задание остаётся; повтор не создаёт пустой повторный коммит.
При ручной правке опубликованного файла проверка хеша останавливает отправку
до разбора конфликта. Force push и автоматический rebase не используются.
Голосовое подтверждение означает сохранение в vault, а не завершение push.

Статус доступен через защищённый `GET /v1/knowledge/git` host-side agent service:
`idle`, `busy`, `synced`, `retry`, `conflict`, `disabled`. Результат `synced`
включает SHA коммита. Ошибка Git не блокирует последующие голосовые записи.
